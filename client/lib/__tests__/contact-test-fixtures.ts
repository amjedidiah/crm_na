import { churches, events, ministries } from "@/lib/mock-data";
import type { ContactListings } from "@/lib/contact-query";

export const mockContactListings: ContactListings = {
  churches,
  ministries,
  events,
};
